node_modules/
dist/
.editor-preview.html
icon-report.txt
assets/icon.original.ico

# Step pictures are Blizzard's, so they are kept out of the repository. Only
# manifest.json stays; fetch the images with tools/fetch-icons.js or from the app.
assets/icons/*.png

# Replays are personal, so they are kept out of the repository.
replay/
tools/__pycache__/
.venv-replay/
replay/out/
.claude/

# Builds are personal, so they are kept out of the repository. Exactly one
# sample ships in seed/ for the release build.
builds/
